import {ADD_COMMENT, REMOVE_COMMENT, THUMB_UP, THUMB_DOWN} from "../actions/comments-actions";

const comments = (store = [], action) => {
    switch (action.type) {

    }
};

export default comments;
